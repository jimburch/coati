name: Back Merge

on:
  workflow_run:
    # Trigger on both the web app release and the CLI release so that
    # version-bump commits (cli/package.json) propagate to develop promptly.
    workflows: ["Release", "CLI Release"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch: # manual trigger for hotfix backmerges

jobs:
  back-merge:
    name: Merge main into develop
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_WORKFLOWS }}

      - name: Merge main into develop
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout develop
          git merge origin/main --no-edit
          git push origin develop
