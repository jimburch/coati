---
description: Testing conventions for Atlas UI components and hooks.
globs:
  - "**/*.test.ts"
  - "**/*.test.tsx"
alwaysApply: false
---

# Testing

Atlas uses Vitest + React Testing Library. Tests colocate with source.

## Test file layout

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Button } from './Button';

describe('Button', () => {
  it('renders the label', () => {
    render(<Button>Save</Button>);
    expect(screen.getByRole('button', { name: 'Save' })).toBeInTheDocument();
  });

  it('invokes onClick when clicked', async () => {
    const user = userEvent.setup();
    const onClick = vi.fn();
    render(<Button onClick={onClick}>Save</Button>);

    await user.click(screen.getByRole('button', { name: 'Save' }));
    expect(onClick).toHaveBeenCalledOnce();
  });

  it('does not invoke onClick when disabled', async () => {
    const user = userEvent.setup();
    const onClick = vi.fn();
    render(<Button disabled onClick={onClick}>Save</Button>);

    await user.click(screen.getByRole('button', { name: 'Save' }));
    expect(onClick).not.toHaveBeenCalled();
  });

  it('forwards the ref to the underlying button', () => {
    const ref = { current: null } as React.RefObject<HTMLButtonElement>;
    render(<Button ref={ref}>Save</Button>);
    expect(ref.current).toBeInstanceOf(HTMLButtonElement);
  });

  it('merges className with internal variant classes', () => {
    render(<Button className="custom">Save</Button>);
    const btn = screen.getByRole('button');
    expect(btn.className).toContain('custom');
    expect(btn.className).toContain('bg-primary');
  });
});
```

## Queries

Prefer in this order:

1. `getByRole` with an accessible name (covers a11y + behavior)
2. `getByLabelText` for form fields
3. `getByPlaceholderText` only when no label exists (bad a11y, usually means a fix is needed first)
4. `getByText` for static content
5. `getByTestId` only as a last resort, and with a comment justifying it

## Rules

- Use `userEvent.setup()` once per test; do not share across tests
- `await` every `userEvent.*` call — they are async
- Prefer assertions that describe behavior (`toBeInTheDocument`, `toBeDisabled`) over implementation (`toHaveClass`)
- Use `toEqual` over `toBe` for objects and arrays
- Every bug fix adds a regression test that fails on the old code
- Tests read as sentences: `"does not invoke onClick when disabled"` — not `"test disabled"`

## Hooks

For custom hooks, use `renderHook` from `@testing-library/react`:

```tsx
import { renderHook, act } from '@testing-library/react';
import { useCounter } from './useCounter';

it('increments the count', () => {
  const { result } = renderHook(() => useCounter());
  act(() => result.current.increment());
  expect(result.current.count).toBe(1);
});
```

## Don't

- Don't use `container.querySelector` — use RTL queries
- Don't test Radix internals — trust the library
- Don't mock `@testing-library/*`
- Don't use `it.only` or `describe.only` in committed code
- Don't rely on visual order with `container.firstChild` — query by role
